// We could have store current state directly in App main component
// But doing so makes it easy to add new features

const state = {
  messages: [],
  personnalities: ['Me', 'Myself', 'I']
}

export default state
